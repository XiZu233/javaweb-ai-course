# RAG 制度知识库问答

## 学习目标

- 理解 RAG（检索增强生成）的核心原理
- 掌握文档分块、向量检索、上下文增强的技术链路
- 实现一个企业制度知识库问答系统

## 核心知识点

### 1. 什么是 RAG

RAG（Retrieval-Augmented Generation）是一种将信息检索与大语言模型生成结合的技术。

**为什么需要 RAG？**
- 大模型有知识截止日期，不了解企业私有信息
- 大模型容易"幻觉"，编造不存在的内容
- RAG 让大模型基于检索到的真实文档回答问题

### 2. RAG 三阶段流程

```
文档摄入阶段：
  PDF/Word 文档 → 文本提取 → 分块 → Embedding → 向量存储

查询阶段：
  用户提问 → Embedding → 向量相似度检索 → Top-K 相关片段

生成阶段：
  检索片段 + 用户问题 → Prompt 增强 → LLM 生成回答
```

### 3. 文档分块策略

```java
// 按段落分块（简化版）
String[] paragraphs = content.split("\\n\\n");
for (String para : paragraphs) {
    if (para.length() > 20) {
        chunks.add(para.trim());
    }
}

// 进阶：按 Token 数分块（推荐512 tokens/块，128 overlap）
// 使用 TokenTextSplitter
```

### 4. Prompt 增强模板

```java
String prompt = """
    你是企业制度知识库助手。请基于以下提供的参考文档片段回答问题。
    如果片段中没有相关信息，请明确说明"根据现有资料无法回答"。
    不要编造信息，不要引用片段中不存在的内容。

    参考文档片段：
    [片段1] 年假天数根据工龄计算...
    [片段2] 请假需提前3个工作日...

    用户问题：年假怎么请？
    """;
```

### 5. 前端设计

- 悬浮 AI 助手球（右下角）
- 点击展开聊天窗口
- Markdown 渲染回答内容
- 显示参考来源

## 动手练习

1. 在 `tlias-pro-module-ai` 中完成 `RagService` 的实现
2. 准备一份测试用的制度文档（员工手册片段）
3. 调用 `/ai/rag/ingest` 接口摄入文档
4. 测试问答："年假怎么请？"、"报销需要什么材料？"

## 进阶思考

- 如何评估 RAG 系统的回答质量？
- 如果企业有上千份制度文档，如何高效管理和检索？
- 如何防止用户通过提问绕过权限看到敏感信息？

## 本节小结

RAG 的本质是"给大模型配上一本参考书"。检索决定回答的准确性，Prompt 工程决定回答的格式和约束。
